---
name: Checks

on: [push]

jobs:
  test-lint:
    name: Test and Lint
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@master

      - name: Tests
        run: "pipenv python manage.py test"

      - name: Lint
        run: "flake8"
